apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: physical-ai-platform-demo
  namespace: openshift-gitops
  labels:
    environment: demo
    profile: full
spec:
  project: default

  source:
    repoURL: https://github.com/redhat-et/physical-ai-platform-demo.git
    targetRevision: main
    path: platform/overlays/demo

  destination:
    server: https://kubernetes.default.svc
    namespace: physical-ai

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
    - group: datasciencecluster.opendatahub.io
      kind: DataScienceCluster
      jsonPointers:
        - /status
    - group: serving.kserve.io
      kind: InferenceService
      jsonPointers:
        - /status
    - group: apps
      kind: StatefulSet
      name: maas-db
      jsonPointers:
        - /spec/replicas
