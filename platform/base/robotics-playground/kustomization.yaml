apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # yamllint disable-next-line rule:line-length
  - https://github.com/redhat-et/robotics-playground//deploy/kustomize/robotics-playground-ui?ref=main
  - route-rerun.yaml

namespace: redhat-ods-applications

patches:
  - path: patches/nginx-proxy-targets.yaml
