apiVersion: maas.opendatahub.io/v1alpha1
kind: MaaSSubscription
metadata:
  name: pi05-subscription
  namespace: models-as-a-service
spec:
  owner:
    groups:
      - name: system:authenticated
  modelRefs:
    - name: pi05
      namespace: physical-ai-models
      tokenRateLimits:
        - limit: 100000
          window: 1h
