apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pi05-model-cache
  namespace: physical-ai-models
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: gp3-csi
