apiVersion: v1
kind: Service
metadata:
  name: dreamzero-metrics
  namespace: physical-ai-models
  labels:
    monitoring.opendatahub.io/scrape: "true"
    name: dreamzero-metrics
spec:
  ports:
  - name: vllm-omni-runtime-metrics
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    serving.kserve.io/inferenceservice: dreamzero
  type: ClusterIP
