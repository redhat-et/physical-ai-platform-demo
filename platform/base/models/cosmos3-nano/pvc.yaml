apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cosmos3-model-cache
  namespace: physical-ai-models
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: gp3-csi
