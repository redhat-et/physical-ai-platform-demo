apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qwen3-omni-model-cache
  namespace: physical-ai-models
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 80Gi
  storageClassName: gp3-csi
