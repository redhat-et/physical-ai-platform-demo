apiVersion: v1
kind: Service
metadata:
  name: platform-agent
  namespace: physical-ai
  labels:
    app: platform-agent
spec:
  selector:
    app: platform-agent
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
