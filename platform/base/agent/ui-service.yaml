apiVersion: v1
kind: Service
metadata:
  name: platform-agent-ui
  namespace: redhat-ods-applications
  labels:
    app: platform-agent-ui
spec:
  selector:
    app: platform-agent-ui
  ports:
    - name: http-mf
      port: 8080
      targetPort: http-mf
      protocol: TCP
