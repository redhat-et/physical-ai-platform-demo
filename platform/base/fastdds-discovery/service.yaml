apiVersion: v1
kind: Service
metadata:
  name: fastdds-discovery
  namespace: physical-ai
  labels:
    app.kubernetes.io/name: fastdds-discovery
spec:
  selector:
    app.kubernetes.io/name: fastdds-discovery
  ports:
    - name: discovery
      port: 11811
      targetPort: discovery
      protocol: UDP
