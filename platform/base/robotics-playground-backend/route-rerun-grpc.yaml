apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: robotics-playground-rerun-grpc
  namespace: physical-ai
spec:
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  to:
    kind: Service
    name: robotics-playground
  port:
    targetPort: rerun-grpc
